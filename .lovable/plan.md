

## Update Email Entry ID

Replace the placeholder `entry.EMAIL_PLACEHOLDER` in `RSVPSection.tsx` with the actual ID `entry.123718265`.

### Change
**RSVPSection.tsx** (line 18): Change `"entry.EMAIL_PLACEHOLDER"` to `"entry.123718265"`

That's it — one line change and the email field will be properly submitted to your Google Form.

